spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:property_db}
    username: ${POSTGRES_USER:property}
    password: ${POSTGRES_PASSWORD:property}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect